var assert = require("assert");
var inventory = require("../main.js");

describe("Stock", function(){
    it("should display the inventory", function(){
        assert.equal(inventory, (itemName, itemCount));
    });
});
